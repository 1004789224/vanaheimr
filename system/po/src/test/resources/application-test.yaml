datasource:
  db:
    username: sa
    password: sa
    url: jdbc:h2:mem:myapp
ebean:
  currentUserProvider: io.vanaheimr.common.db.provider.RequestContextUserProvider
  test:
    platform: h2 # h2, postgres, mysql, oracle, sqlserver
    ddlMode: dropCreate # none | dropCreate | migrations | create
    dbName: myapp